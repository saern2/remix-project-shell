# Pinned to what was verified: kokoro-onnx 0.4.9's session seams and the two
# Adapt fixes were confirmed against this exact version's source (Round B,
# Phase 0). onnxruntime is the CPU build; the concurrency model (one session,
# intra_op=1, N threads) was measured on 1.29.
onnxruntime==1.29.0
kokoro-onnx==0.4.9
numpy>=2.0,<3
aiohttp>=3.12,<4
requests>=2.32,<3
bullmq==3.0.5
redis>=5,<8
