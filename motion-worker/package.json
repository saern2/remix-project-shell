{
  "name": "motion-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SaaS explainer generation via Anymotion, bring-your-own-key. Sibling of render-worker and tts-worker; shares only the box's Redis, under bull:motion:* keys.",
  "scripts": {
    "start": "node src/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@anymotion-agent/anymotion": "1.1.1",
    "bullmq": "^5.12.0",
    "express": "^4.19.2",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
